Se puede agregar un:
@ModelAsttrubute('users')
public List<User> userModel(){
    List<User>users = Arrays.asList(......)
    return users
}

En thymeleaf podemos usarlo para llamar la informacion de los usuarios con "users"

------------------------
obtener informacion con  httpServletRequest
@GetMapping ("/request")
public ParamMixDTO request (httpServletRequest request){

    ParamMixDTO params = new ParamMixDTO();
    paramas.setCode(Integer.parseInt(request,getParameter("code"))) // devuelve String
    return params

}